{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "parcel src/*.pug --lazy",
    "build": "tsc && parcel build src/index.pug",
    "preview": "parcel build src/index.pug --reporter @parcel/reporter-bundle-analyzer"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "devDependencies": {
    "@parcel/optimizer-cssnano": "^2.6.2",
    "@parcel/reporter-bundle-analyzer": "^2.6.2",
    "@parcel/transformer-pug": "^2.6.2",
    "parcel": "^2.6.2",
    "tailwindcss": "^3.1.6",
    "typescript": "^4.6.4"
  }
}
